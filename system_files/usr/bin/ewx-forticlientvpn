#!/usr/bin/env bash

# Check if cookie was captured
if [ -z "$VPN_URL" ]; then
    echo '$VPN_URL not populated'
    exit 1
fi

sudo openfortivpn $VPN_URL:$VPN_PORT \
    --saml-login \
    --trusted-cert $VPN_CERT_SIGNATURE2 \
    --set-dns=1 \
    --pppd-use-peerdns=0 \

